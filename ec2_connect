- name: Test Docker on EC2
  uses: appleboy/ssh-action@v1.0.0
  with:
    host: 13.202.46.178
    username: ec2-user
    key: ${{ secrets.EC2_SSH_KEY }}
    script: |
      docker version
      docker info
      docker ps -a
